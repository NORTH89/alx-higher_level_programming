// Select the header element using the querySelector function
const header = document.querySelector('header');

// Update the text color of the header element to red (#FF0000)
header.style.color = '#FF0000';
